import React from 'react'

function FunctionProps(props) {
  return (
    <div>
    <h2>Hello, My Name is {props.name}. My qualification is {props.class}</h2>
    </div>
  )
}

export default FunctionProps;
